// Feature: expense-budget-visualizer
// Add and delete transaction logic.
// Full implementation in task 7.

/**
 * @typedef {{ id: string, name: string, amount: number, category: string, timestamp: number }} Transaction
 */

export function addTransaction(name, amount, category) {
  // Stub — full implementation in task 7.
}

export function deleteTransaction(id) {
  // Stub — full implementation in task 7.
}
